-- RM98575.USERS definition
CREATE TABLE RM98575.USERS (
	"ID" CHAR(36) NOT NULL ENABLE,
	"NAME" VARCHAR2(100) NOT NULL ENABLE,
	"CPF" VARCHAR2(11),
	"IMAGE" BLOB,
	"BIRTHDATE" DATE NOT NULL ENABLE,
	"CHILDRENNUMBER" NUMBER(*, 0),
	"EMAIL" VARCHAR2(100) NOT NULL ENABLE,
	"PASSWORD" VARCHAR2(2000) NOT NULL ENABLE,
	"BALANCE" NUMBER(38, 0),
	"MONTHLYSPENDINGSLIMIT" NUMBER(*, 0),
	"INVESTMENTPROFILELEVEL" NUMBER(38, 0),
	"ACTIVE" NUMBER(*, 0) NOT NULL ENABLE,
	"CREATEDAT" DATE NOT NULL ENABLE,
	"UPDATEDAT" DATE,
	CONSTRAINT "PK_USERS" PRIMARY KEY ("ID")
);

-- RM98575.ADDRESS definition
CREATE TABLE "RM98575"."ADRRESS" (
	"ID" CHAR(36) NOT NULL ENABLE,
	"USERID" CHAR(36) NOT NULL ENABLE,
	"CEP" VARCHAR2(8) NOT NULL ENABLE,
	"HOUSENUMBER" VARCHAR2(20) NOT NULL ENABLE,
	"STREET" VARCHAR2(100) NOT NULL ENABLE,
	"CITY" VARCHAR2(100) NOT NULL ENABLE,
	"STATE" VARCHAR2(50) NOT NULL ENABLE,
	"COUNTRY" VARCHAR2(50) NOT NULL ENABLE,
	"ACTIVE" NUMBER(*, 0) NOT NULL ENABLE,
	"CREATEDAT" DATE NOT NULL ENABLE,
	"UPDATEDAT" DATE,
	CONSTRAINT "PK_ADDRESS" PRIMARY KEY ("ID")
);

ALTER TABLE
	"RM98575"."ADRRESS"
ADD
	CONSTRAINT "FK_ADRRESS_USER_ID" FOREIGN KEY ("USERID") REFERENCES "RM98575"."USERS" ("ID") ENABLE;

-- RM98575.BANKS definition
CREATE TABLE "RM98575"."BANKS" (
	"ID" CHAR(36) NOT NULL ENABLE,
	"USERID" CHAR(36) NOT NULL ENABLE,
	"NAME" VARCHAR2(60) NOT NULL ENABLE,
	"BANKACCOUNTNUMBER" NUMBER(10, 0) NOT NULL ENABLE,
	"BANKAGENCYNUMBER" NUMBER(10, 0) NOT NULL ENABLE,
	"INTEGRATIONTOKEN" VARCHAR2(200) NOT NULL ENABLE,
	"ACTIVE" NUMBER(*, 0) NOT NULL ENABLE,
	"CREATEDAT" DATE NOT NULL ENABLE,
	"UPDATEDAT" DATE,
	CONSTRAINT "PK_BANKS" PRIMARY KEY ("ID")
);

ALTER TABLE
	"RM98575"."BANKS"
ADD
	CONSTRAINT "FK_BANKS_USER_ID" FOREIGN KEY ("USERID") REFERENCES "RM98575"."USERS" ("ID") ENABLE;

-- RM98575.INVESTMENTS definition
CREATE TABLE "RM98575"."INVESTMENTS" (
	"ID" CHAR(36) NOT NULL ENABLE,
	"USERID" CHAR(36) NOT NULL ENABLE,
	"STOCKID" VARCHAR2(10) NOT NULL ENABLE,
	"AMOUNT" NUMBER(19, 0) NOT NULL ENABLE,
	"ACTIVE" NUMBER(*, 0) NOT NULL ENABLE,
	"CREATEDAT" DATE NOT NULL ENABLE,
	"UPDATEDAT" DATE,
	CONSTRAINT "PK_INVESTMENTS" PRIMARY KEY ("ID")
);

ALTER TABLE
	"RM98575"."INVESTMENTS"
ADD
	CONSTRAINT "FK_INVESTMENTS_USER_ID" FOREIGN KEY ("USERID") REFERENCES "RM98575"."USERS" ("ID") ENABLE;

-- RM98575.JOBS definition
CREATE TABLE "RM98575"."JOBS" (
	"ID" CHAR(36) NOT NULL ENABLE,
	"USERID" CHAR(36) NOT NULL ENABLE,
	"COMPANYNAME" VARCHAR2(50) NOT NULL ENABLE,
	"TITLE" VARCHAR2(60) NOT NULL ENABLE,
	"NETSALLARY" NUMBER(19, 0) NOT NULL ENABLE,
	"ACTIVE" NUMBER(*, 0) NOT NULL ENABLE,
	"CREATEDAT" DATE NOT NULL ENABLE,
	"UPDATEDAT" DATE,
	CONSTRAINT "PK_JOBS" PRIMARY KEY ("ID"));

ALTER TABLE
	"RM98575"."JOBS"
ADD
	CONSTRAINT "FK_JOBS_USER_ID" FOREIGN KEY ("USERID") REFERENCES "RM98575"."USERS" ("ID") ENABLE;

-- RM98575.MOVIMENTATIONS definition
CREATE TABLE "RM98575"."MOVIMENTATIONS" (
	"ID" CHAR(36) NOT NULL ENABLE,
	"USERID" CHAR(36) NOT NULL ENABLE,
	"TITLE" VARCHAR2(50) NOT NULL ENABLE,
	"AMOUNT" NUMBER(*, 0) NOT NULL ENABLE,
	"CATEGORY" VARCHAR2(2000) NOT NULL ENABLE,
	"MOVTYPE" VARCHAR2(2000) NOT NULL ENABLE,
	"ACTIVE" NUMBER(*, 0) NOT NULL ENABLE,
	"CREATEDAT" DATE NOT NULL ENABLE,
	"UPDATEDAT" DATE,
	"DATE" DATE DEFAULT SYSDATE NOT NULL ENABLE,
	CONSTRAINT "PK_MOVIMENTATIONS" PRIMARY KEY ("ID"));

ALTER TABLE
	"RM98575"."MOVIMENTATIONS"
ADD
	CONSTRAINT "FK_MOVIMENTATIONS_USER_ID" FOREIGN KEY ("USERID") REFERENCES "RM98575"."USERS" ("ID") ENABLE;

-- RM98575.PAYMENT_CARDS definition
CREATE TABLE "RM98575"."PAYMENT_CARDS" (
	"ID" CHAR(36) NOT NULL ENABLE,
	"USERID" CHAR(36) NOT NULL ENABLE,
	"CARDNUMBER" VARCHAR2(16) NOT NULL ENABLE,
	"NICKNAME" VARCHAR2(40) NOT NULL ENABLE,
	"EXPIRATIONDATE" DATE NOT NULL ENABLE,
	"CVV" VARCHAR2(3),
	"PAYMENTNETWORK" VARCHAR2(2000) NOT NULL ENABLE,
	"PAYMENTTYPE" VARCHAR2(2000) NOT NULL ENABLE,
	"ACTIVE" NUMBER(*, 0) NOT NULL ENABLE,
	"CREATEDAT" DATE NOT NULL ENABLE,
	"UPDATEDAT" DATE,
	CONSTRAINT "PK_PAYMENT_CARDS" PRIMARY KEY ("ID")
);

ALTER TABLE
	"RM98575"."PAYMENT_CARDS"
ADD
	CONSTRAINT "FK_PAYMENT_CARDS_USER_ID" FOREIGN KEY ("USERID") REFERENCES "RM98575"."USERS" ("ID") ENABLE;

-- RM98575.RECURRENTS definition
CREATE TABLE "RM98575"."RECURRENTS" (
	"ID" CHAR(36) NOT NULL ENABLE,
	"MOVIMENTATIONID" CHAR(36) NOT NULL ENABLE,
	"STARTDATE" DATE NOT NULL ENABLE,
	"ENDDATE" DATE NOT NULL ENABLE,
	"DAYSOFWEEK" VARCHAR2(2000),
	"ISMONTHLY" NUMBER(1, 0) NOT NULL ENABLE,
	"ISANNUAL" NUMBER(1, 0) NOT NULL ENABLE,
	"ACTIVE" NUMBER(*, 0) NOT NULL ENABLE,
	"CREATEDAT" DATE NOT NULL ENABLE,
	"UPDATEDAT" DATE,
	CONSTRAINT "PK_RECURRENTS" PRIMARY KEY ("ID")
);

ALTER TABLE
	"RM98575"."RECURRENTS"
ADD
	CONSTRAINT "FK_RECURRENTS_MOVIMENTATION_ID" FOREIGN KEY ("MOVIMENTATIONID") REFERENCES "RM98575"."MOVIMENTATIONS" ("ID") ENABLE;